# CLAUDE.md — 專案規則

## README 同步規則

修改 `jira-time-tracker/` 目錄下的任何檔案（腳本、設定範本、安裝流程）時，
**必須同步檢查並更新 `jira-time-tracker/README.md`**，確保文件與實際行為一致。

需特別注意的項目：
- `setup.sh` 的互動提示文字、預設值、安裝流程
- `scripts/` 下腳本的功能描述
- 前置需求與相依套件
